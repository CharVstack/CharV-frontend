name: main action
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: update repo
        run : ssh development@${{ env.development_server_ip }} CD/init.sh

